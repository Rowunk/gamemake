{
  "name": "gamemake",
  "version": "0.0.1",
  "description": "Foundations for a browser-native game engine: core math, slab allocator, time; WebGPU/WebGL2 rendering primitives + static demos. No runtime deps.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "start:open": "npm run start --silent & sleep 1; (xdg-open http://localhost:8000/public/ || open http://localhost:8000/public/ || start http://localhost:8000/public/ || true)",
    "serve": "make serve",
    "serve:open": "make serve-open",
    "test": "node --test",
    "cov": "make cov",
    "clean": "make clean"
  },
  "engines": { "node": ">=18" },
  "license": "UNLICENSED",
  "keywords": ["webgpu", "game-engine", "ecs", "math", "allocator"],
  "author": "",
  "repository": { "type": "git", "url": "" }
}
